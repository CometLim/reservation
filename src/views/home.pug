doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Reservation
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body
        header
            h1 Reservation Page
        main
            div#welcome
                form#roomName
                    input(placeholder="room name", required, type="text")
                    button Enter Room
                form#nickname
                    input(placeholder="room nickname", requiredm type="text")
                    button send
            div#room
                h3
                ul
                form#name
                    input(placeholder="room nickname", requiredm, type="text")
                    button send
                form#msg
                    input(placeholder="message", requiredm, type="text")
                    button send
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/app.js")